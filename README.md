# GUI-Executable
An update for the GUI, packed the python script into executable program.

I used pyinstaller to pack this python script.

Every time we run this program, a folder called "MyQualtricsDownload" will pop up on the desktop.

The program is under "dist", called "GUI-3-1"
